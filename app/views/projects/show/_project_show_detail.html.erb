<%=
new_somethings = {
	title: @project.project_name,
	edit: edit_project_path(@project),
	value: [
		[ link_to("レポート", :controller => "reports", :action => "show", project_id: @project.id) ]
	]
}

render partial: "shared/partials/basic_table_section", locals: { table_section: new_somethings }
%>