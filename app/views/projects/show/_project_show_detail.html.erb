<%=
new_somethings = {
	title: @report.project.project_name,
	edit: edit_project_path(@report.project),
	value: [
		[ link_to("レポート", :controller => "reports", :action => "show", project_id: @report.project.id) ]
	]
}

render partial: "shared/partials/basic_table_section", locals: { table_section: new_somethings }
%>