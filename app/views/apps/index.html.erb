<% current_customer.apps.each do |app| %>
	<h1><%= link_to app.app_name, app_path(app.id) %></h1>
	<% app.projects.each do |project| %>
		<p><%= link_to project.project_name, project_path(project.id) %></p>
	<% end %>
<% end %>