<% @emotions.each do |e| %>
  <div class="report_tips">
    <div class="tip_emo_kind_box">
      <span><%= e.name %></span>
    </div>
    <% @tips.each do |t| %>
      <%= render partial: "reports/show/report_tip" , locals: { tip: t }%>
    <% end %>
  </div>
<% end %>
