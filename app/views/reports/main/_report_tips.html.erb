<% @emotions.each do |e| %>
  <div class="report_tips <%= e.id%>_tips">
    <div class="tip_emo_kind_box">
      <span><%= e.name %></span>
    </div>
    <% @tips.each do |t| %>
      <%= render partial: "reports/main/report_tip" , locals: { tip: t, emo: e }%>
    <% end %>
  </div>
<% end %>
