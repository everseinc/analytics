version: '2'
services:
  db:
    image: mysql
    environment:
    ports:
    - "3306:3306"
    MYSQL_ROOT_PASSWORD: test
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/analytics
    ports:
      - "3000:3000"
    depends_on:
      - db
