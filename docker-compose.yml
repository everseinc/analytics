version: '2'
services:
  db:
    image: everse/analytics-mysql
    environment:
      MYSQL_ROOT_PASSWORD: test
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/analytics
    ports:
      - "3000:3000"
    stdin_open: true
    depends_on:
      - db
